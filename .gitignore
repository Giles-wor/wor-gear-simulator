node_modules
dist
.env
.DS_Store
ref_fig/
*.jpg
*.jpeg
*.png
wor-webapp.zip

# 공략 탭 이미지는 리포에 포함 (위 *.jpg/*.png 무시 규칙의 예외)
!guide/assets/
!guide/assets/**

# 유출정보 탭 이미지도 리포에 포함
!leaks/assets/
!leaks/assets/**

# 길드원 페이지 평문 콘텐츠는 리포에 올리지 않음 (암호화본 content.encrypted.json 만 커밋)
guild/content.source.json

# sync-banners 크롤 실패 시 생성되는 디버그 덤프 (리포에 올리지 않음)
banners/data/_debug_fetch.txt

# 마병 아이콘은 리포에 포함 (위 *.jpg/*.png 무시 규칙의 예외)
!guild/assets/
!guild/assets/**
